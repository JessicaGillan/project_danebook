<div class="row">
  <div class="col-xs-12">
    <% flash.each do |message_type, message| %>
    <div class="alert alert-<%= message_type == "error" ?  "warning" : message_type %>"><%= message %></div>
    <% end %>
  </div>
</div>
<% flash.discard %>
